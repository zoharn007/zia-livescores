properties([parameters([string('BACKEND_IMAGE_NAME')])])

pipeline {
    agent any

    stages {
        stage('Backend Deploy') {
            steps
                    sh '''
                    print("ok")
                    '''
        }
    }
}